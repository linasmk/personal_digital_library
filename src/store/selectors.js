/* =================================
=========== SELECTORS ================
==================================== */
export default (books) => {
  return books.map((book) => {
    return book;
  });
};
